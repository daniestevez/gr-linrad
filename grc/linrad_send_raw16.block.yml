# auto-generated by grc.converter

id: linrad_send_raw16
label: Send RAW 16bit
category: '[linrad]'

parameters:
-   id: ip
    label: IP
    dtype: string
    default: '"127.0.0.1"'
-   id: base_port
    label: Base port
    dtype: int
    default: '50000'
-   id: passband_center
    label: Passband center
    dtype: float
-   id: bufsize
    label: Buffer size
    dtype: int
    default: '0x1000'

inputs:
-   domain: stream
    dtype: complex

templates:
    imports: import linrad
    make: linrad.send_raw16(${ip}, ${base_port}, ${passband_center}, ${bufsize})
    callbacks:
    - set_frequency(${passband_center})

file_format: 1
