# auto-generated by grc.converter

id: linrad_server
label: Linrad Net Server
category: '[linrad]'

parameters:
-   id: ip
    label: Listen IP
    dtype: string
    default: '"0.0.0.0"'
-   id: samp_rate
    label: Sample rate
    dtype: float
    default: samp_rate
-   id: ad
    label: AD channels
    dtype: int
    default: '2'
-   id: rf
    label: RF channels
    dtype: int
    default: '1'
-   id: buf
    label: Buffer size
    dtype: int
    default: '0x1000'
-   id: iq
    label: IQ data
    dtype: bool
    default: 'True'
-   id: dword
    label: DWORD_INPUT (set to false for 16bit)
    dtype: bool
    default: 'True'

templates:
    imports: import linrad
    make: linrad.linrad_server(listen_ip=${ip}, samp_rate=${samp_rate}, ad_channels=${ad},
        rf_channels=${rf}, bufsize=${buf}, iq_data=${iq}, dword_input=${dword})

file_format: 1
